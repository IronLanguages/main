fails:Math#sin returns the sine of the passed argument expressed in radians
fails:Math#sin returns the sine for Complex numbers
fails:Math.sin returns the sine of the passed argument expressed in radians
fails:Math.sin returns the sine for Complex numbers
fails:"Math#sin returns the sine of the passed argument expressed in radians"
fails:"Math#sin returns the sine for Complex numbers"
fails:"Math.sin returns the sine of the passed argument expressed in radians"
fails:"Math.sin returns the sine for Complex numbers"
